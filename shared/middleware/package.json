{
  "name": "shared-middleware",
  "version": "1.0.0",
  "description": "Shared middleware for microservices platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "shared-types": "workspace:*",
    "shared-utils": "workspace:*",
    "hono": "^3.12.0",
    "pino": "^8.16.0"
  }
}